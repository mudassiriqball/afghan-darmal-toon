import baseUrl from './baseUrl'

export default {
    LOGIN: baseUrl + '/api/users/login-user',
    SIGNUP: baseUrl + '/api/users/signup',

    // Slider
    ADD_SLIDER: baseUrl + '/sliders/add-slider',

    // Category
    ADD_CATEGORY: baseUrl + '/categories/category',
    ADD_HOME_CATEGORY: baseUrl + '/categories/home-category'
}
